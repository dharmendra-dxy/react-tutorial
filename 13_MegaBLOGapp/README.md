# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh


## Dependecies using while creating this app:

1. [AppWrite](https://appwrite.io/) used for backend as a service.

2. [TinyMCE](https://www.npmjs.com/package/tinymce) react component used for text editor. Visit [Gihtub](https://github.com/tinymce/tinymce)

3. [React Hook Form](https://react-hook-form.com/) used for handing and creating forms.

4. [Html-React-Parser](https://www.npmjs.com/package/html-react-parser)


## Notes for app:

1. create a file: conf(of config for .env variables ).
2. Create a folder "appwrite" where backend mehods will be written.
3. Create a store using redux for managing state.

